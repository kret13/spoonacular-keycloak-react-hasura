import React from "react";
import Layout from "../Layout/Layout";

const NotFoundPage: React.FC = () => {
  return (
    <Layout>
      <div className="container">not found page </div>
    </Layout>
  );
};

export default NotFoundPage;
