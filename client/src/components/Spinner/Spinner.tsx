import React from "react";

export type HeightOptions = "fullHeight";

const Spinner: React.FC<{ height?: HeightOptions }> = ({ height }) => {
  return <div>Spinner...</div>;
};
export default Spinner;
